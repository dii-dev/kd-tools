/**
 * Custom hook for using language context
 * 
 * This hook provides access to the language context and ensures it's being used
 * within a LanguageProvider component.
 */
"use client"

export { useLanguage } from "@/lib/language-context"
